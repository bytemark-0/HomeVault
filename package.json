{
  "name": "homevault",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "fixtures:backup": "tsc -p tsconfig.tests.json && node .build/tests/scripts/generateSampleBackup.js",
    "mobile": "npm run start --workspace=apps/mobile",
    "test": "npm run test:packages && npm run test --workspace=apps/mobile",
    "test:packages": "tsc -p tsconfig.tests.json && node .build/tests/scripts/generateSampleBackup.js && node .build/tests/tests/exportPackage.test.js && node .build/tests/tests/repository.test.js && node .build/tests/tests/taskUtils.test.js",
    "test:mobile": "npm run test --workspace=apps/mobile",
    "typecheck": "npm run typecheck:packages && npm run typecheck --workspace=apps/mobile",
    "typecheck:app": "npm run typecheck --workspace=apps/mobile",
    "typecheck:packages": "tsc --noEmit -p tsconfig.packages.json",
    "lint": "eslint 'packages/*/src/**/*.ts' 'apps/mobile/src/**/*.{ts,tsx}' 'tests/**/*.ts'",
    "lint:fix": "eslint --fix 'packages/*/src/**/*.ts' 'apps/mobile/src/**/*.{ts,tsx}' 'tests/**/*.ts'"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.61.0",
    "eslint": "^9.39.4",
    "typescript": "~6.0.3"
  }
}
